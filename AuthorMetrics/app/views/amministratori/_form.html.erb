<%= form_with(model: amministratore) do |form| %>
  <% if amministratore.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(amministratore.errors.count, "error") %> prohibited this amministratore from being saved:</h2>

      <ul>
        <% amministratore.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
