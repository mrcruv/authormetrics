<%= form_with(model: scritto) do |form| %>
  <% if scritto.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(scritto.errors.count, "error") %> prohibited this scritto from being saved:</h2>

      <ul>
        <% scritto.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
