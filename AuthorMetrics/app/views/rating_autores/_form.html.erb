<%= form_with(model: rating_autore) do |form| %>
  <% if rating_autore.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(rating_autore.errors.count, "error") %> prohibited this rating_autore from being saved:</h2>

      <ul>
        <% rating_autore.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
