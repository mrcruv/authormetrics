<%= form_with(model: autore_preferito) do |form| %>
  <% if autore_preferito.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(autore_preferito.errors.count, "error") %> prohibited this autore_preferito from being saved:</h2>

      <ul>
        <% autore_preferito.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
