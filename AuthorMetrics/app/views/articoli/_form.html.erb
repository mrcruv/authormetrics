<%= form_with(model: articolo) do |form| %>
  <% if articolo.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(articolo.errors.count, "error") %> prohibited this articolo from being saved:</h2>

      <ul>
        <% articolo.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
