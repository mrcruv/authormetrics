<%= form_with(model: articolo_preferito) do |form| %>
  <% if articolo_preferito.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(articolo_preferito.errors.count, "error") %> prohibited this articolo_preferito from being saved:</h2>

      <ul>
        <% articolo_preferito.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
