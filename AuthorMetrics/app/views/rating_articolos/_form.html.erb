<%= form_with(model: rating_articolo) do |form| %>
  <% if rating_articolo.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(rating_articolo.errors.count, "error") %> prohibited this rating_articolo from being saved:</h2>

      <ul>
        <% rating_articolo.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
