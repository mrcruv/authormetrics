<%= form_with(model: [@user, @favorite_author]) do |form| %>
  <% if favorite_author.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(favorite_author.errors.count, "error") %> prohibited this favorite_author from being saved:</h2>

      <ul>
        <% favorite_author.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :author_id%>
    <%= form.text_field :author_id, value: $author_info.author_id %>
    <%= form.submit %>
  </div>

<% end %>
