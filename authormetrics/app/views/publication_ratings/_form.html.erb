<%= form_with(model: [@publication, @publication_rating]) do |form| %>
  <% if publication_rating.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(publication_rating.errors.count, "error") %> prohibited this publication rating from being saved:</h2>

      <ul>
        <% publication_rating.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :rating %>:
    <%= form.number_field :rating, style: "width:50px" %>
    <%= form.submit %>
  </div>
<% end %>