<div id="<%= dom_id favorite_publication %>">
  <p>
    <strong>Publication:</strong>
    <%a=Publication.find(favorite_publication.publication_id)%>
    <%= link_to a.title, publication_path(a) %>
  </p>
</div>
