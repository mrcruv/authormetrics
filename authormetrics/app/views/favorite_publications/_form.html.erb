<%= form_with(model: [@user, @favorite_publication]) do |form| %>
  <% if favorite_publication.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(favorite_publication.errors.count, "error") %> prohibited this favorite_publication from being saved:</h2>

      <ul>
        <% favorite_publication.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :publication_id, style: "display: block" %>
    <%= form.text_field :publication_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
