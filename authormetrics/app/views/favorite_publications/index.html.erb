<p style="color: green"><%= notice %></p>

<div class='margine'>
<h1>Favorite publications</h1>

<div id="favorite_publications">
  <ul>
  <% @favorite_publications.each do |favorite_publication| %>
    <li><%= render favorite_publication %>
    <%= button_to "Show", publication_path(favorite_publication.publication_id), :method=>:get %>
    <%= button_to 'Destroy', [User.find(favorite_publication.user_id),favorite_publication ], :method=>:delete %>
    <br></br>
  <% end %>
  </ul>
</div>

<%= button_to "New favorite publication", new_user_favorite_publication_path, :method=>:get %>
<%= button_to "HOME", home_path, :method=>:get%>
<%if user_signed_in?%>
  <%= button_to "MY ACCOUNT", dashboard_path, :method=>:get%>
<%end%>
</div>
