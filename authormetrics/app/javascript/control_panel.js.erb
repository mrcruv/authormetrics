function deleteComment(row_id, user_id, publication_id){
    var row = document.getElementById(row_id);
    row.parentNode.removeChild(row);
}


function deleteReview(row_id, user_id, author_id){
    var row = document.getElementById(row_id);
    row.parentNode.removeChild(row);
}